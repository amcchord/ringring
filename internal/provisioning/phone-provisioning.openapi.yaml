openapi: 3.1.2
info:
  title: RingRing Phone API
  version: 1.2.0
  summary: One-time party joining, SIP setup, live call menus, and background-call registration.
  description: |
    This API lets a trusted native phone application either finish a general
    RingRing invitation or consume an already-configured phone setup link. Both
    flows return one SIP-over-TLS account and a bounded snapshot of buttons the
    phone may call. It never provides a PSTN route, a public directory, or
    a persistent directory credential.

    Every token is a secret bearer value. A phone invitation may be previewed
    without consuming it, then one successful claim consumes it atomically. A
    phone provisioning token is consumed by its one successful GET. Clients must
    not log, crawl, or disclose token-bearing URLs. Store returned SIP passwords
    only in device-protected secret storage. Token and credential responses
    intentionally do not enable browser CORS. After setup, a native phone may
    use that SIP username and password as HTTP Basic credentials over HTTPS to
    refresh its private call menu and register its Apple VoIP device token.
  license:
    name: MIT
    url: https://github.com/amcchord/ringring/blob/main/LICENSE
servers:
  - url: /
    description: The RingRing deployment that issued the setup link.
tags:
  - name: Phone invitations
  - name: Phone provisioning
  - name: Configured phones
paths:
  /openapi.yaml:
    get:
      operationId: getOpenAPIDocument
      summary: Download this OpenAPI description.
      responses:
        "200":
          description: The public, non-secret API description.
          content:
            application/yaml:
              schema:
                type: string
  /api/v1/phone-invitations/{token}:
    get:
      operationId: previewPhoneInvitation
      tags: [Phone invitations]
      summary: Preview a general one-time invitation without consuming it.
      description: |
        Returns only the party name and one currently available extension
        suggestion. The suggestion is advisory; a concurrent claim can take it
        before this client posts. A `410` response deliberately does not reveal
        whether a token is malformed, unknown, canceled, expired, or already used.
      parameters:
        - $ref: "#/components/parameters/InvitationToken"
      responses:
        "200":
          description: The minimal details needed to render native setup.
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store, max-age=0
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhoneInvitationPreview"
        "410":
          $ref: "#/components/responses/InvitationUnavailable"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: claimPhoneInvitation
      tags: [Phone invitations]
      summary: Choose an extension and atomically claim a general invitation.
      description: |
        Creates exactly one member and device, consumes the invitation, and
        returns that device's SIP credential and same-party call-menu snapshot in
        one transaction-backed flow. The high-entropy path token authenticates
        this JSON request; clients must not attach cookies or browser credentials.
      parameters:
        - $ref: "#/components/parameters/InvitationToken"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhoneInvitationClaim"
      responses:
        "200":
          $ref: "#/components/responses/PhoneProvisioning"
        "400":
          description: The JSON or requested phone details are invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "409":
          description: Another member claimed the requested extension first. The invitation remains usable.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "410":
          $ref: "#/components/responses/InvitationUnavailable"
        "413":
          description: The request exceeds the small bounded invitation payload size.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "415":
          description: The request is not application/json.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/phone-provisioning/{token}:
    get:
      operationId: consumePhoneProvisioning
      tags: [Phone provisioning]
      summary: Consume one phone setup token.
      description: |
        Returns the complete setup document exactly once. A `410` response is
        deliberately generic: the token may be malformed, unknown, expired,
        already used, replaced, or attached to a revoked phone.
      parameters:
        - $ref: "#/components/parameters/ProvisioningToken"
      responses:
        "200":
          $ref: "#/components/responses/PhoneProvisioning"
        "410":
          $ref: "#/components/responses/ProvisioningUnavailable"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"
  /provision/ios/{token}:
    get:
      operationId: consumeLegacyIOSProvisioning
      tags: [Phone provisioning]
      summary: Consume one phone setup token through the RingRing iOS compatibility route.
      description: |
        Backward-compatible alias used by released RingRing iOS builds. New
        clients should use `/api/v1/phone-provisioning/{token}`. Both paths
        consume the same token, so only the first successful request can work.
      deprecated: true
      parameters:
        - $ref: "#/components/parameters/ProvisioningToken"
      responses:
        "200":
          $ref: "#/components/responses/PhoneProvisioning"
        "410":
          description: The setup link is unavailable. The compatibility route returns a generic HTML error page.
          content:
            text/html:
              schema:
                type: string
        "429":
          description: Too many provisioning requests from this source.
          headers:
            Retry-After:
              schema:
                type: string
                const: "60"
          content:
            text/plain:
              schema:
                type: string
        "500":
          description: The server could not safely build the setup document. The compatibility route returns a generic HTML error page.
          content:
            text/html:
              schema:
                type: string
  /api/v1/phone/state:
    get:
      operationId: getPhoneState
      tags: [Configured phones]
      summary: Refresh a configured phone's private menu and live-call buttons.
      description: |
        Authenticates one active device with its SIP username and password, then
        returns a bounded current directory and currently joinable party calls.
        The response is a menu snapshot, not presence or call history.
      security:
        - phoneCredential: []
      responses:
        "200":
          description: Current extension and private call buttons.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PhoneState"
        "401":
          $ref: "#/components/responses/PhoneUnauthorized"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"
  /api/v1/phone/push:
    put:
      operationId: registerAppleVoIPToken
      tags: [Configured phones]
      summary: Register or replace this device's Apple VoIP push token.
      description: |
        Stores the token encrypted at rest for this authenticated active device.
        RingRing sends Apple only an opaque call UUID; party names, member names,
        extensions, SIP credentials, and call audio never enter the APNs payload.
      security:
        - phoneCredential: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhonePushRegistration"
      responses:
        "204":
          description: The current token is registered.
        "400":
          description: The token or APNs environment is invalid.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "401":
          $ref: "#/components/responses/PhoneUnauthorized"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          description: This server has not configured Apple background-call delivery.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
    delete:
      operationId: unregisterAppleVoIPToken
      tags: [Configured phones]
      summary: Stop background-call pushes to this configured device.
      security:
        - phoneCredential: []
      responses:
        "204":
          description: Any stored token for this device is removed.
        "401":
          $ref: "#/components/responses/PhoneUnauthorized"
        "429":
          $ref: "#/components/responses/TooManyRequests"
components:
  securitySchemes:
    phoneCredential:
      type: http
      scheme: basic
      description: The configured device's globally unique SIP username and SIP password, sent only over HTTPS.
  parameters:
    InvitationToken:
      name: token
      in: path
      required: true
      description: A 43-character, high-entropy, single-use bearer token from a general RingRing invitation link.
      schema:
        type: string
        minLength: 43
        maxLength: 43
        pattern: "^[A-Za-z0-9_-]{43}$"
    ProvisioningToken:
      name: token
      in: path
      required: true
      description: A 43-character, high-entropy, single-use bearer token supplied privately by the RingRing host.
      schema:
        type: string
        minLength: 43
        maxLength: 43
        pattern: "^[A-Za-z0-9_-]{43}$"
  responses:
    PhoneProvisioning:
      description: The one-time SIP account and same-party call-menu snapshot.
      headers:
        Cache-Control:
          description: Credential responses must never be cached.
          schema:
            type: string
            const: no-store, max-age=0
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/PhoneProvisioningDocument"
          examples:
            phoneSetup:
              summary: Example with fictitious credentials and labels.
              value:
                version: 1
                sip:
                  server: ringring.example
                  port: 5061
                  transport: tls
                  username: "483726"
                  password: "927461830572"
                  extension: "101"
                destinations:
                  - kind: person
                    label: Studio phone
                    dial: "102"
                  - kind: service
                    label: Echo test
                    detail: Hear your own voice come back.
                    dial: "*10"
    ProvisioningUnavailable:
      description: The setup token cannot be used. No reason is disclosed.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    InvitationUnavailable:
      description: The invitation token cannot be used. No reason is disclosed.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    PhoneUnauthorized:
      description: The device credential is invalid or revoked.
      headers:
        WWW-Authenticate:
          schema:
            type: string
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    TooManyRequests:
      description: Too many provisioning requests from this source.
      headers:
        Retry-After:
          schema:
            type: string
            const: "60"
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    InternalError:
      description: The server could not safely build the setup document.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
  schemas:
    PhoneInvitationPreview:
      type: object
      additionalProperties: false
      required: [version, party_name, suggested_extension]
      properties:
        version:
          type: integer
          const: 1
        party_name:
          type: string
          minLength: 1
          maxLength: 80
          description: The host-chosen party name already shown by the web invitation.
        suggested_extension:
          type: string
          pattern: "^[0-9]{2,5}$"
          description: An advisory, currently unoccupied party extension.
    PhoneInvitationClaim:
      type: object
      additionalProperties: false
      required: [display_name, extension]
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 40
          description: Friendly name shown only inside this private party.
        extension:
          type: string
          pattern: "^[0-9]{2,5}$"
          description: Requested party extension. Reserved public emergency and crisis numbers are rejected.
        device_label:
          type: string
          minLength: 1
          maxLength: 40
          description: Optional host-facing device label. Defaults to Phone app.
    PhoneProvisioningDocument:
      type: object
      required: [version, sip, destinations]
      properties:
        version:
          type: integer
          const: 1
          description: Reject unsupported versions rather than guessing at semantics.
        sip:
          $ref: "#/components/schemas/SIPAccount"
        destinations:
          type: array
          maxItems: 128
          description: An initial setup snapshot. Configured clients can refresh it through the authenticated state endpoint.
          items:
            $ref: "#/components/schemas/Destination"
    PhoneState:
      type: object
      additionalProperties: false
      required: [version, extension, destinations]
      properties:
        version:
          type: integer
          const: 1
        extension:
          type: string
          pattern: "^[0-9]{2,5}$"
          description: This member's current extension, including changes made with the phone keypad.
        destinations:
          type: array
          maxItems: 128
          description: Current same-party people, enabled services, and live calls this phone may join.
          items:
            $ref: "#/components/schemas/Destination"
    PhonePushRegistration:
      type: object
      additionalProperties: false
      required: [token, environment]
      properties:
        token:
          type: string
          minLength: 64
          maxLength: 64
          pattern: "^[a-f0-9]{64}$"
          description: Lowercase hexadecimal encoding of the 32-byte PushKit VoIP token.
        environment:
          type: string
          enum: [production, development]
          description: Must match the server's configured APNs environment. TestFlight uses production.
    SIPAccount:
      type: object
      required: [server, port, transport, username, password, extension]
      properties:
        server:
          type: string
          minLength: 1
          maxLength: 253
          description: DNS name or IP literal of the RingRing SIP server.
        port:
          type: integer
          const: 5061
        transport:
          type: string
          const: tls
        username:
          type: string
          minLength: 1
          maxLength: 128
          pattern: "^[A-Za-z0-9_-]+$"
          description: SIP authentication identity. It is not the party extension.
        password:
          type: string
          minLength: 1
          maxLength: 256
          description: Sensitive SIP password. Reject control characters and keep it only in protected secret storage.
        extension:
          type: string
          pattern: "^[0-9]{2,5}$"
          description: The display and short-dial extension assigned inside this party.
    Destination:
      type: object
      required: [kind, label, dial]
      properties:
        kind:
          type: string
          enum: [person, service, call]
        label:
          type: string
          minLength: 1
          maxLength: 80
          description: Friendly button label with no leading/trailing whitespace or control characters.
        detail:
          type: string
          maxLength: 160
          description: Optional explanatory text with no leading/trailing whitespace or control characters.
        dial:
          type: string
          pattern: "^(?:[0-9]{2,5}|\\*[0-9]{2}|\\*16[0-9]{2,5})$"
          description: Hidden same-party extension, explicit RingRing service code, or party-conference join code for a kind=call entry. It is never a public telephone number.
    Problem:
      type: object
      required: [type, title, status, detail]
      properties:
        type:
          type: string
          format: uri-reference
          const: about:blank
        title:
          type: string
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
